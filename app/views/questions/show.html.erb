<div>Test: <%= @question.test.title %> (test_id:<%= @question.test.id %>)</div>
<div>Question <strong><%= @question.body %></strong>
  <span>
    <%= link_to "Delete", question_path, method: :delete,
                                         data: {confirm: "Are you sure?"} %>
  </span>
</div>
