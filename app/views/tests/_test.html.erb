<tr>
  <td><%= test.title %></td>
  <td><%= test.questions.count %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.category.title %></td>
  <td><%= button_to t('.btn_start'), start_test_path(test), class: 'btn btn-outline-primary' %></td>
</tr>
