<%= link_new_test %> | <%= link_to_gists %> | <%= link_to_badges %>
<hr>

<h1><%= t('.header') %></h1>

<%= render 'shared/errors', resource: @test %>

<%= render partial: 'admin/tests/test_table', locals: { test: @tests } %>
